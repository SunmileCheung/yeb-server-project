<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxxx.server.mapper.EmployeeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xxxx.server.pojo.Employee">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="gender" property="gender" />
        <result column="birthday" property="birthday" />
        <result column="idCard" property="idCard" />
        <result column="wedlock" property="wedlock" />
        <result column="nationId" property="nationId" />
        <result column="nativePlace" property="nativePlace" />
        <result column="politicId" property="politicId" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="address" property="address" />
        <result column="departmentId" property="departmentId" />
        <result column="jobLevelId" property="jobLevelId" />
        <result column="posId" property="posId" />
        <result column="engageForm" property="engageForm" />
        <result column="tiptopDegree" property="tiptopDegree" />
        <result column="specialty" property="specialty" />
        <result column="school" property="school" />
        <result column="beginDate" property="beginDate" />
        <result column="workState" property="workState" />
        <result column="workID" property="workID" />
        <result column="contractTerm" property="contractTerm" />
        <result column="conversionTime" property="conversionTime" />
        <result column="notWorkDate" property="notWorkDate" />
        <result column="beginContract" property="beginContract" />
        <result column="endContract" property="endContract" />
        <result column="workAge" property="workAge" />
        <result column="salaryId" property="salaryId" />
    </resultMap>

    <resultMap id="EmployeeWithSalary" type="com.xxxx.server.pojo.Employee"
               extends="BaseResultMap">
        <association property="salary" javaType="com.xxxx.server.pojo.Salary">
            <id column="sid" property="id"/>
            <result column="sbasicSalary" property="basicSalary"/>
            <result column="sbonus" property="bonus"/>
            <result column="slunchSalary" property="lunchSalary"/>
            <result column="strafficSalary" property="trafficSalary"/>
            <result column="sallSalary" property="allSalary"/>
            <result column="spensionBase" property="pensionBase"/>
            <result column="spensionPer" property="pensionPer"/>
            <result column="smedicalBase" property="medicalBase"/>
            <result column="smedicalPer" property="medicalPer"/>
            <result column="saccumulationFundBase"
                    property="accumulationFundBase"/>
            <result column="saccumulationFundPer"
                    property="accumulationFundPer"/>
            <result column="sname" property="name"/>
        </association>
        <association property="department"
                     javaType="com.xxxx.server.pojo.Department">
            <result column="dname" property="name"/>
        </association>
    </resultMap>

    <resultMap id="SalarySearch" type="com.xxxx.server.pojo.Employee" extends="BaseResultMap">
        <result column="ename" property="name"/>
        <association property="salary" javaType="com.xxxx.server.pojo.Salary">
            <result column="basicSalary" property="basicSalary"/>
            <result column="allSalary" property="allSalary"/>
        </association>
        <association property="salarySearch" javaType="com.xxxx.server.pojo.SalarySearch">
            <result column="subsidy" property="subsidy"/>
            <result column="fund" property="fund"/>
            <result column="dif" property="dif"/>
        </association>
        <association property="salaryAdjust" javaType="com.xxxx.server.pojo.SalaryAdjust">
            <result column="afterSalary" property="afterSalary"/>
        </association>
        <association property="department" javaType="com.xxxx.server.pojo.Department">
            <result column="dname" property="name"/>
        </association>

     </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, gender, birthday, idCard, wedlock, nationId, nativePlace, politicId, email, phone, address, departmentId, jobLevelId, posId, engageForm, tiptopDegree, specialty, school, beginDate, workState, workID, contractTerm, conversionTime, notWorkDate, beginContract, endContract, workAge, salaryId
    </sql>
    <!--获取所有员工账套-->
    <select id="getEmployeeWithSalary" resultMap="EmployeeWithSalary">
        SELECT
            e.*,
            d.`name` AS dname,
            s.id AS sid,
            s.`name` AS sname,
            s.basicSalary AS sbasicSalary,
            s.trafficSalary AS strafficSalary,
            s.lunchSalary AS slunchSalary,
            s.bonus AS sbonus,
            s.allSalary AS sallSalary,
            s.pensionPer AS spensionPer,
            s.pensionBase AS spensionBase,
            s.medicalPer AS smedicalPer,
            s.medicalBase AS smedicalBase,
            s.accumulationFundPer AS saccumulationFundPer,
            s.accumulationFundBase AS saccumulationFundBase
        FROM
            t_employee AS e
                LEFT JOIN t_salary AS s ON e.salaryId = s.id
                LEFT JOIN t_department AS d ON e.departmentId = d.id
        ORDER BY
            e.id
    </select>
    <!---->
    <!--查询是否有员工存在-->
    <select id="selectByName" parameterType="string" resultType="com.xxxx.server.pojo.Employee">
        select name from t_employee where name=#{name}
    </select>
    <!--多条件查询员工薪资-->
    <select id="getSalarySearch" parameterType="string" resultMap="SalarySearch">
        SELECT
            e.`name` ename,
            s.basicSalary basicSalary,
            (s.lunchSalary + s.trafficSalary ) subsidy ,
            (s.pensionBase + s.medicalBase + s.accumulationFundBase ) fund,
            s.allSalary allSalary,
            ( afterSalary - basicSalary - trafficSalary - lunchSalary - pensionBase - medicalBase - accumulationFundBase ) dif,
            sa.afterSalary afterSalary ,
            d.`name` dname
        FROM
            t_employee e
                LEFT JOIN t_salary_adjust sa ON e.id = sa.eid
                LEFT JOIN t_salary s ON e.salaryId = s.id
                LEFT JOIN t_department d ON e.departmentId = d.id
        where
            1=1
        <if test="ename != null and ename!=''">
            and e.name=#{ename}
        </if>
        <if test="dname != null and dname!=''">
            and d.name=#{dname}
        </if>
    </select>


</mapper>
